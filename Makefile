# sudo docker compose build --build-arg POSTGRES_HOST=$POSTGRES_HOST --build-arg POSTGRES_PORT=$POSTGRES_PORT --build-arg POSTGRES_NAME=$POSTGRES_NAME --build-arg POSTGRES_USER=$POSTGRES_USER --build-arg POSTGRES_PASSWORD=$POSTGRES_PASSWORD


# сборка проекта в образ (контейнер)
# сохранение образа в архив
# деплой на сервер
# распаковка на сервере
# очистка старых образов
